import { DisableArrowKeysDirective } from './disable-arrow-keys.directive';

describe('DisableArrowKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableArrowKeysDirective();
    expect(directive).toBeTruthy();
  });
});
